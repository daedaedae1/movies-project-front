import 'package:flutter/material.dart';

class CollaborativeRecommendPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('협업 필터링 추천'),
      ),
      body: Center(
        child: Text('여기에 협업 필터링 추천 영화가 표시됩니다.'),
      ),
    );
  }
}
